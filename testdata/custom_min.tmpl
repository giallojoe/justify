# tiny custom template to prove overrides flow through
build:
	@echo CMAKE={{index .CMakeDirs 0}}
program:
	@echo CARGO={{.CargoBinGuess}}
node:
	@echo NODES={{range .NodeEntries}}{{.}}|{{end}}
go:
	@echo GOEXE={{range .GoExeCandidates}}{{.}}|{{end}}
attach:
	@echo ATTACH={{.AttachOnDev}}
